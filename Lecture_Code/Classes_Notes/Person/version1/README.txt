In main.cpp, we define a class Person. The rule is that name and year of birth cannot be changed, 
once the object is created, though they can be read. The main() program illustrates the
creation of two persons, p1 and p2, and use of methods, getname and getage on them.
