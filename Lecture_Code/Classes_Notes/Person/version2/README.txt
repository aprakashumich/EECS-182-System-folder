The main difference from version 1 is that the class definition is moved to a header file. That way,
it can be used by multiple .cpp files. 
In Person.h, we define a class Person. It is used by main.cpp.
